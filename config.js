// PDI / MIA landing page configuration
// Fill in these three values, then redeploy. Nothing else needs editing.
//
//  1. SUPABASE_URL and SUPABASE_ANON_KEY:
//       Supabase dashboard > Project Settings > API.
//       The anon key is safe to be public WHEN Row Level Security is on
//       (see step 2 in README.md). It only lets visitors add themselves
//       to the waitlist, not read it.
//
//  2. CALENDLY_URL:
//       Your Calendly scheduling link, e.g. https://calendly.com/yourname/demo

window.PDI_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY",
  CALENDLY_URL: "https://calendly.com/REPLACE-ME/demo"
};
